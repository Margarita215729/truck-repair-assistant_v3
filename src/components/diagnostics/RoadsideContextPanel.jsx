import React from 'react';

export default function RoadsideContextPanel({ context, onChange }) {
  // Simplified — mode selector, VIN and fault codes removed.
  // VIN is now entered via TruckSelector; fault codes via ErrorCodeInput tool.
  return null;
}
